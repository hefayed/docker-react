language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t hefayed/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true hefayed/docker-react npm run test
